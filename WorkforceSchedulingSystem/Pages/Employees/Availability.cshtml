@page "{id:guid}"
@model API.Pages.Employees.AvailabilityModel
@{
    ViewData["Title"] = "Availability · " + (Model.Employee?.FullName ?? "");
}

<a asp-page="/Employees/Index" class="btn btn-sm btn-outline-secondary mb-3">&larr; Back to Employees</a>

<h1 class="mb-0">@Model.Employee?.FullName</h1>
<p class="text-muted mb-4">Weekly availability windows — used by the auto-scheduler</p>

<div class="row g-4">
    <div class="col-lg-7">
        <div class="card shadow-sm border-0">
            <div class="card-header bg-white fw-semibold">Current Windows</div>
            <table class="table align-middle mb-0">
                <thead class="table-light">
                    <tr>
                        <th>Day</th>
                        <th>Start</th>
                        <th>End</th>
                        <th class="text-end">Actions</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var availability in Model.Availabilities.OrderBy(window => window.DayOfWeek).ThenBy(window => window.StartTime))
                    {
                        <tr>
                            <td>@availability.DayOfWeek</td>
                            <td>@availability.StartTime.ToString(@"hh\:mm")</td>
                            <td>@availability.EndTime.ToString(@"hh\:mm")</td>
                            <td class="text-end">
                                <form method="post" asp-page-handler="Delete" class="d-inline"
                                      onsubmit="return confirm('Remove this availability window?');">
                                    <input type="hidden" name="availabilityId" value="@availability.Id" />
                                    <button type="submit" class="btn btn-sm btn-outline-danger">Delete</button>
                                </form>
                            </td>
                        </tr>
                    }
                    @if (!Model.Availabilities.Any())
                    {
                        <tr><td colspan="4" class="text-center text-muted py-4">No windows set yet.</td></tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
    <div class="col-lg-5">
        <div class="card shadow-sm border-0">
            <div class="card-header bg-white fw-semibold">Add Availability</div>
            <div class="card-body">
                <form method="post" asp-page-handler="Add">
                    <div class="mb-3">
                        <label class="form-label">Day of Week</label>
                        <select asp-for="NewDayOfWeek" class="form-select">
                            <option value="1">Monday</option>
                            <option value="2">Tuesday</option>
                            <option value="3">Wednesday</option>
                            <option value="4">Thursday</option>
                            <option value="5">Friday</option>
                            <option value="6">Saturday</option>
                            <option value="0">Sunday</option>
                        </select>
                    </div>
                    <div class="mb-3">
                        <label class="form-label">Start Time</label>
                        <input type="time" name="NewStartTime" class="form-control"
                               value="@Model.NewStartTime.ToString(@"hh\:mm")" required />
                    </div>
                    <div class="mb-3">
                        <label class="form-label">End Time</label>
                        <input type="time" name="NewEndTime" class="form-control"
                               value="@Model.NewEndTime.ToString(@"hh\:mm")" required />
                    </div>
                    <button type="submit" class="btn btn-primary w-100">Add Availability</button>
                </form>
            </div>
        </div>
    </div>
</div>
